version: "3.9"

services:
  api:
    image: ${DOCKERHUB_USERNAME}/diplom:master
    restart: always
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
    expose:
      - 8000


